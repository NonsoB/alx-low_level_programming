#include "main.h"
/**
 * print_alphabet- a function that prints the alphabets
 * "a" - "z" in small letters
 * Return: 0 (success)
 */

void print_alphabet(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)

	{
		_putchar(letter);
	}
	_putchar('\n');
	return (0);
}

